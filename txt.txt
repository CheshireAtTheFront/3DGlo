Создали папку src -> в нутри нее папку modules, два модуля(burger, script) поместили в папку modules

Webpack инструмент сборки не только модулей, но и проектов

Сборка модуле через Webpack

npm init - эта команда инициализирует проект, то есть определяет его, -y(флажек y) - означает согласиться с всеми настройками по умолчанию

package.json - это основной файл описания проекта, в нем указываются все библиотеки, которые используются